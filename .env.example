# Flask
SECRET_KEY=change-me-to-a-random-string
FLASK_DEBUG=true

# Database
DB_FILE=feeds.db

# Default admin user (only used to seed if no users exist)
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=changeme

# Background feed refresh interval in minutes (0 = disabled, only fetch on login)
FEED_REFRESH_INTERVAL_MINUTES=0
